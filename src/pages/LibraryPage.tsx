import HomePage from './HomePage';

/**
 * The Library page is the same as the Home page — rows of bookshelves.
 * Both / and /library show the same experience.
 */
export default function LibraryPage() {
  return <HomePage />;
}
